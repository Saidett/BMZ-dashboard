[project]
name = "climate-commitments-dashboard"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "requests",
    "pdfplumber",
    "ollama",
    "pandas",
]

[tool.setuptools.packages.find]
where = ["."]
